# Shopware LLM wiki (test fixture)

Navigate with the shell: `cat platform/index.md`, `ls platform/dev/6.7`, `grep -rn term platform/dev/6.7`, `cat platform/dev/6.7/…/page.md`.
Ids are wiki-root-relative paths starting with the layer name.
