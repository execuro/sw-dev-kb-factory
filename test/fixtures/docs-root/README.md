# docs-root fixture

Fixture root that mimics the shape of the `docs` corpus (two top-level layers,
`developer` and `merchant`, each with its own `index.md`, and no `manifest.json`).
Used by `registry.test.ts` and `server.test.ts` to exercise `kb.config.json`
corpus selection.
